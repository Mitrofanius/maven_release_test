name: component-release

on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    if: github.actor != 'maven-release-automation[bot]'
    runs-on: ubuntu-latest

    steps:
      - name: Generate a token
        id: generate-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: Checkout
        uses: actions/checkout@v6
        with:
          token: ${{ steps.generate-token.outputs.token }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      # ============================================================
      # PREPARE RELEASE
      # ============================================================

      - name: Infer release and next development version
        id: version
        run: |
          CURRENT_VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout | sed 's/-SNAPSHOT//')
          echo "Current development version: $CURRENT_VERSION"
          
          IFS='.' read -r major minor patch <<< "$CURRENT_VERSION"
          
          RELEASE_VERSION=$CURRENT_VERSION
          NEXT_VERSION="${major}.${minor}.$((patch + 1))"

          echo "release_version=$RELEASE_VERSION"
          echo "next_version=$NEXT_VERSION"
          
          echo "release_version=$RELEASE_VERSION" >> $GITHUB_OUTPUT
          echo "next_version=$NEXT_VERSION" >> $GITHUB_OUTPUT

      - name: Create and checkout release branch
        id: release-branch
        run: |
          BRANCH_NAME="release/${{ steps.version.outputs.release_version }}"
          echo "release_branch=$BRANCH_NAME" >> $GITHUB_OUTPUT
          git checkout -b $BRANCH_NAME

      - name: Set release version in pom.xml
        run: |
          mvn versions:set -DnewVersion=${{ steps.version.outputs.release_version }} -DgenerateBackupPoms=false

      - name: Update changelog for release
        run: |
          VERSION="${{ steps.version.outputs.release_version }}"
          perl -pi -e "s/## \{\{NEW_RELEASE_VERSION_PLACEHOLDER\}\}/## $VERSION/" CHANGELOG.md

      - name: Commit release version
        run: |
          git add pom.xml CHANGELOG.md
          git commit -m "release ${{ steps.version.outputs.release_version }}"
          git push --set-upstream origin ${{ steps.release-branch.outputs.release_branch }}

      - name: Create and merge Release Pull Request
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          gh pr create \
            --title "release ${{ steps.version.outputs.release_version }}" \
            --body "This PR prepares release ${{ steps.version.outputs.release_version }}" \
            --base test-workflow-on-push \
            --head ${{ steps.release-branch.outputs.release_branch }}
          
          git fetch --all
          
          gh pr merge --rebase ${{ steps.release-branch.outputs.release_branch }} --delete-branch

      # ============================================================
      # BUILD & PUBLISH RELEASE
      # ============================================================

      - name: Create and push tag
        run: |
          VERSION="${{ steps.version.outputs.release_version }}"
          git tag "release/$VERSION"
          git push origin "release/$VERSION"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          VERSION="${{ steps.version.outputs.release_version }}"
          # Extract the content for this version from CHANGELOG.md
          # Gets everything between "## VERSION" and the next "##" heading
          CHANGELOG_ENTRY=$(sed -n "/## $VERSION/,/^## /p" CHANGELOG.md | sed '1d;$d' | sed '/^$/d')
          gh release create "release/$VERSION" \
            --title "release/$VERSION" \
            --notes "$CHANGELOG_ENTRY" \
            --generate-notes

      # ============================================================
      # POST-RELEASE (NEXT DEVELOPMENT VERSION)
      # ============================================================

      - name: Create and checkout post-release branch
        id: post-release-branch
        run: |
          BRANCH_NAME="post-release/${{ steps.version.outputs.release_version }}"
          echo "post_release_branch=$BRANCH_NAME" >> $GITHUB_OUTPUT
          git checkout -b $BRANCH_NAME

      - name: Set next snapshot version
        run: |
          mvn versions:set -DnewVersion="${{ steps.version.outputs.next_version }}-SNAPSHOT" -DgenerateBackupPoms=false

      - name: Add new changelog placeholder
        run: |
          printf "## {{NEW_RELEASE_VERSION_PLACEHOLDER}}\n\n%s" "$(cat CHANGELOG.md)" > CHANGELOG.md.tmp
          mv CHANGELOG.md.tmp CHANGELOG.md

      - name: Commit next snapshot version
        run: |
          git add pom.xml CHANGELOG.md
          git commit -m "post-release/${{ steps.version.outputs.release_version }}"
          git push --set-upstream origin ${{ steps.post-release-branch.outputs.post_release_branch }}

      - name: Create Post-Release Pull Request
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          gh pr create \
            --title "Post-release: Set next development version ${{ steps.version.outputs.next_version }}-SNAPSHOT" \
            --body "This PR sets up the next development version after release ${{ steps.version.outputs.release_version }}" \
            --base test-workflow-on-push \
            --head ${{ steps.post-release-branch.outputs.post_release_branch }}

      - name: Merge Post-Release Pull Request
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          gh pr merge --rebase ${{ steps.post-release-branch.outputs.post_release_branch }} --delete-branch
